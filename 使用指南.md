# ScreenWall 屏幕墙 · 功能说明与使用指南

> **版本**：v1.2.9（服务端版本号显示在左上角"屏幕墙"后）
> **客户端版本**：客户端运行时右键菜单显示

---

## 一、产品简介

ScreenWall 是一款远程屏幕监控工具，支持同时查看多台设备的实时画面，并可对在线设备发起远程键盘控制。

---

## 二、界面布局

### 2.1 主界面
- **左上角**：显示"屏幕墙"标题及服务端版本号
- **右上角**：远控列表按钮（🖥️）和设置按钮（⚙️）
- **中间区域**：设备格子，支持 1 / 4 / 16 画面切换
- **设备格子**：显示设备名称、在线/离线状态、截图预览

### 2.2 预览弹窗（点击任意格子）
打开后显示设备实时画面，右上角显示控制按钮区：
- **浮动关闭按钮**（右上角）：可随时关闭预览
- **文字提示**：鼠标/键盘操作说明
- **🖥️ 显示器**：多显示器切换按钮
- **⌨️ 键盘**：远控开关（绿色开启 / 灰色关闭）
- **1080p**：高清滑动开关（绿色开启 / 灰色关闭）
- **UU 按钮**：远程协助入口

---

## 三、核心功能详解

### 3.1 多画面监控
- 支持 **1 / 4 / 16** 画面切换，点击切换按钮即可
- 设备离线时格子灰显并显示"离线"
- 自愈机制：设备实际在线但显示离线时，收到截图帧后自动恢复

### 3.2 预览控制按钮区

#### 显示器切换（🖥️）
- 多显示器设备可点击切换显示不同屏幕
- 按钮显示当前显示器编号

#### 键盘控制（⌨️）
- 点击开启远控 → 服务端启动键盘控制客户端
- 再次点击关闭 → 关闭键盘控制客户端
- **按钮状态**：绿色 = 已开启，灰色 = 已关闭
- 设备离线时按钮变灰不可点击

#### 1080p 高清模式
- 滑动开关样式：开启滑块滑到右侧 + 绿色背景
- 开启后设备截图切换为高清模式（1080p 分辨率）
- 关闭预览弹窗自动关闭高清模式
- 支持多显示器（通过显示器切换选择）

#### 远控列表（🖥️ 按钮）
- 弹出所有设备一览的网格列表
- 绿色边框表示远控已开启
- 设备名后显示当前客户端版本号
- 离线设备按钮变灰，点击后记录预期状态，上线自动执行

### 3.3 预览操作说明

| 操作 | 功能 |
|------|------|
| 鼠标左键点击 | 点击屏幕（远程点击） |
| Shift + 右键点击 | 鼠标右键 |
| Alt + 鼠标滚轮下 | 向下滚动 |
| Alt + Shift + 鼠标滚轮上 | 向上滚动 |
| Shift + 键盘按键 | 切换大小写和字符 |

### 3.4 报警功能

#### 报警触发
- 当设备截图画面发生异常变化时自动触发报警
- 系统通过截图内容相似度检测异常

#### 报警预览
- 弹出独立预览窗口显示报警截图
- 右上角同样有浮动关闭按钮
- **UU 按钮**：点击发起远程协助

#### 查重机制
- 同一区域的重复报警会自动过滤，避免骚扰

### 3.5 远程协助（UU）
- 点击 UU 按钮，自动跳转到远程协助页面
- 远程协助需设备在线才能使用

---

## 四、使用流程

### 4.1 启动服务端
```
运行 ScreenWallServer.exe
控制台显示：🖥️ 屏幕墙服务端 v1.2.9 已启动
```

### 4.2 客户端配置
1. 将 ScreenWallClient.exe 放在客户端电脑上运行
2. 右键托盘图标 → 设置 → 配置服务端地址和端口
3. 客户端会自动心跳连接，显示在服务端界面

### 4.3 远程控制
1. 在服务端点击设备格子打开预览
2. 点击"⌨️ 键盘"按钮开启远控
3. 在预览画面上进行远程操作
4. 操作完成后再次点击"⌨️ 键盘"关闭

### 4.4 客户端升级
1. 服务端检测到客户端版本过低时提示升级
2. 点击升级按钮，客户端自动下载更新包
3. 升级过程中请勿关闭客户端窗口

---

## 五、常见问题

### Q：设备显示离线但实际在线？
A：系统会自动自愈，收到截图帧后自动恢复在线状态。

### Q：远控按钮是灰色无法点击？
A：设备离线状态下远控不可用，等待设备上线后自动恢复。

### Q：1080p 高清模式无法开启？
A：确保设备在线，且浏览器预览窗口已打开。

### Q：客户端如何查看版本号？
A：客户端运行时右键托盘图标，菜单标题显示版本号。

---

## 六、版本说明

| 项目 | 版本号位置 |
|------|-----------|
| 服务端 | 运行时控制台 + 左上角界面显示 |
| 客户端 | 运行后右键菜单标题 |
