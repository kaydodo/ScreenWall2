# ScreenWall2 三步优化方案（最终版·可执行）

> **状态**：用户已确认所有纠正，可直接执行。
> **目标**：62 Mbps → 13 Mbps（4列场景，节省79%）
> **实施顺序**：Step 2 → Step 1 → Step 3

---

## 0. 关键纠正与分辨定义

### 确认的纠正
| 项目 | 正确值 | 废弃值 |
|------|--------|--------|
| Level 1 分辨率 | 853×720 | ~~1280×720~~ |
| Level 2 分辨率 | 1280×1080 | ~~960×1080~~ |
| 监控墙基准 | 720p原始 1280×720 | ~~1080p~~ |
| 预览 | 不裁剪，1280×1080即最终尺寸 | ~~需要裁剪~~ |
| 裁剪坐标 | 所有格子有CSS还原能力，服务器直接在压缩帧上按比例裁剪 | ~~需模拟还原再裁剪~~ |

### 分辨率定义
| Level | 分辨率 | 压缩比 | 对应页面 |
|-------|--------|--------|---------|
| 0 | 480×270 | 1:4 (原1920→480) | main.html 主页格子 |
| 1 | 853×720 | 2:3 (原1280→853) | monitor-wall.html 监控墙 |
| 2 | 1280×1080 | 2:3 (原1920→1280) | preview.html 预览大图 |

---

## Step 1: 流级别系统（替换 startHQ/stopHQ/hq1080On/hq1080Off）

### 1.1 目标
用单一 `setLevel` 消息替代4个HQ消息，消除 HQ 计数 bug 根源。

### 1.2 服务端修改（server.js）

在文件顶部附近新增全局变量：
```js
// 在现有 Maps 附近添加（约 line 50-80 区域）
const deviceMaxLevel = new Map();   // deviceId → 0|1|2
const deviceSubscribers = new Map(); // deviceId → Map(ws → level)
```

新增函数（放在全局变量之后，消息处理之前）：
```js
function updateLevel(deviceId) {
    if (!deviceSubscribers.has(deviceId)) return;
    const subs = deviceSubscribers.get(deviceId);
    if (subs.size === 0) {
        deviceMaxLevel.delete(deviceId);
        return;
    }
    let max = 0;
    for (const level of subs.values()) {
        if (level > max) max = level;
    }
    const old = deviceMaxLevel.get(deviceId) || 0;
    deviceMaxLevel.set(deviceId, max);
    if (max !== old) {
        // 通知客户端切换分辨率
        const dev = devices.get(deviceId);
        if (dev && dev.ws && dev.ws.readyState === 1) {
            dev.ws.send(JSON.stringify({ type: 'setLevel', level: max }));
        }
    }
}

function subscribeLevel(deviceId, ws, level) {
    if (!deviceSubscribers.has(deviceId)) {
        deviceSubscribers.set(deviceId, new Map());
    }
    deviceSubscribers.get(deviceId).set(ws, level);
    updateLevel(deviceId);
}

function unsubscribeLevel(deviceId, ws) {
    if (!deviceSubscribers.has(deviceId)) return;
    deviceSubscribers.get(deviceId).delete(ws);
    updateLevel(deviceId);
}

function unsubscribeAllLevel(ws) {
    for (const [deviceId, subs] of deviceSubscribers) {
        if (subs.delete(ws)) {
            updateLevel(deviceId);
        }
    }
}
```

在消息处理中添加 `setLevel` case（找到类似 switch-case 处理消息的地方）：
```js
case 'setLevel':
    // payload: { deviceId, level: 0|1|2 }
    subscribeLevel(data.deviceId, ws, data.level);
    break;
```

在 `ws.on('close')` 处理器中添加清理：
```js
// 在 close handler 中添加
unsubscribeAllLevel(ws);
```

删除以下旧消息处理（或保留但标记废弃）：
- `startHQ` / `stopHQ` / `hq1080On` / `hq1080Off`
- 可保留兼容：旧消息内部转调 subscribeLevel/unsubscribeLevel

### 1.3 客户端修改（client.py）

在消息处理中添加 `setLevel`（找到类似 `if msg_type == 'startHQ':` 的地方）：
```python
elif msg_type == 'setLevel':
    level = msg_data.get('level', 0)
    if level == 0:
        self.target_width = 480
        self.target_height = 270
    elif level == 1:
        self.target_width = 853
        self.target_height = 720
    elif level == 2:
        self.target_width = 1280
        self.target_height = 1080
    # 注意：需要修改截图逻辑使用 target_width/target_height
```

### 1.4 前端修改

**main.html**（替换 `startHQ` 发送）：
```js
// 原来发 startHQ 的地方
ws.send(JSON.stringify({ type: 'setLevel', deviceId, level: 0 }));
```

**monitor-wall.html**（替换 `startHQ` 发送）：
```js
// 原来发 subscribeWall 后还要发 startHQ，现在统一
ws.send(JSON.stringify({ type: 'setLevel', deviceId, level: 1 }));
```

**preview.html**（打开/关闭时）：
```js
// 打开预览
ws.send(JSON.stringify({ type: 'setLevel', deviceId, level: 2 }));
// 关闭预览
ws.send(JSON.stringify({ type: 'setLevel', deviceId, level: 1 })); // 回退
```

---

## Step 2: 监控墙服务端裁剪

### 2.1 目标
监控墙格子只需小尺寸帧，服务端按格子尺寸裁剪，减少每个格子的带宽。

### 2.2 裁剪公式（已确认）
```
crop_x_compressed = orig_x * (compressed_width / orig_width)
例：720p 3×3 格子 orig=427×240 → compressed crop=285×240
    浏览器自动将 285×240 拉伸到 427×240 显示（还原能力）
```

### 2.3 监控墙格子尺寸表
| 布局 | 压缩帧 | 每格orig | 每格compressed | 帧大小估 |
|------|--------|----------|----------------|---------|
| 2×2 | 853×720 | 640×360 | 427×360 | ~17KB |
| 3×3 | 853×720 | 427×240 | 285×240 | ~8KB |
| 4×4 | 853×720 | 320×180 | 213×180 | ~5KB |

### 2.4 服务端修改（server.js）

新增裁剪函数（可使用 sharp 或纯 JS 实现，推荐 sharp 已有依赖）：
```js
// 裁剪压缩帧（sharp 实现）
async function cropFrame(frameBuffer, compressedWidth, compressedHeight, targetW, targetH, col, row, cols, rows) {
    const sharp = require('sharp');
    // 原始像素对应的压缩像素比例
    const ratio = compressedWidth / (compressedWidth * (1280 / 853)); // 实际上 ratio = 853/1280 = 2/3
    // 简化：ratio = compressedWidth / originalWidth = 853/1280 = 0.6666
    const origW = compressedWidth / ratio; // 还原原始宽度 1280
    const cellOrigW = Math.floor(origW / cols);
    const cellOrigH = Math.floor(720 / rows); // 高度不变

    // 裁剪坐标（直接用压缩坐标）
    const cellCompW = Math.floor(compressedWidth / cols);
    const cellCompH = Math.floor(compressedHeight / rows);
    const left = col * cellCompW;
    const top = row * cellCompH;

    return sharp(frameBuffer)
        .extract({ left, top, width: cellCompW, height: cellCompH })
        .resize(targetW, targetH, { fit: 'fill' }) // 拉伸到目标尺寸（浏览器会还原比例）
        .webp({ quality: 30 })
        .toBuffer();
}
```

在 `subscribeWall` 处理中添加裁剪逻辑（找到 handleSubscribeWall 附近）：
```js
// 在 subscribeWall 消息处理中，保存布局信息
case 'subscribeWall':
    // ... 现有逻辑
    wallLayouts.set(ws, {
        cols: data.cols || 4,
        rows: data.rows || 4,
        cellW: data.cellW || 320,
        cellH: data.cellH || 180
    });
    // ... 
    break;
```

在帧转发处（找到发送 frame 到监控墙的逻辑）添加裁剪：
```js
// 发送帧给监控墙时（原来直接发送829×720整帧）
for (const [ws, layout] of wallSubscribers) {
    for (let r = 0; r < layout.rows; r++) {
        for (let c = 0; c < layout.cols; c++) {
            const deviceId = layout.deviceIds[r * layout.cols + c];
            if (!deviceId) continue;
            const croppedBuffer = await cropFrame(
                frameBuffer, 853, 720, 
                layout.cellW, layout.cellH, 
                c, r, layout.cols, layout.rows
            );
            ws.send(croppedBuffer); // 二进制发送
        }
    }
}
```

---

## Step 3: 视口懒加载 + 按布局裁剪

### 3.1 目标
主页面200个格子，只接收视口可见+buffer区域的帧，带宽从62Mbps降至13Mbps（79%节省）。

### 3.2 前端修改（main.html）

新增视口追踪代码（放在初始化附近）：
```js
// 视口追踪
const VISIBLE_DEVICE_IDS = new Set();
const VIEWPORT_DEBOUNCE_MS = 300;
let viewportTimer = null;

// IntersectionObserver 检测可见格子
const observer = new IntersectionObserver((entries) => {
    for (const entry of entries) {
        const deviceId = entry.target.dataset.deviceId;
        if (!deviceId) continue;
        if (entry.isIntersecting) {
            VISIBLE_DEVICE_IDS.add(deviceId);
        } else {
            VISIBLE_DEVICE_IDS.delete(deviceId);
        }
    }
    // 加入上下各一行 buffer（需额外计算哪些格子在可见区域上下）
    addBufferRows();
    // debounce上报
    clearTimeout(viewportTimer);
    viewportTimer = setTimeout(reportViewport, VIEWPORT_DEBOUNCE_MS);
}, { root: document.querySelector('.grid-container'), threshold: 0 });

// 上报视口
function reportViewport() {
    ws.send(JSON.stringify({
        type: 'setViewport',
        deviceIds: Array.from(VISIBLE_DEVICE_IDS)
    }));
}

// 在格子创建时绑定
function observeCell(el, deviceId) {
    el.dataset.deviceId = deviceId;
    observer.observe(el);
}

// 清理（beforeunload）
window.addEventListener('beforeunload', () => {
    observer.disconnect();
});
```

按布局裁剪（在 rAF loop 中，根据当前列数裁剪）：
```js
// 根据列数确定裁剪尺寸
const LAYOUT_CROP_SIZES = {
    4:  { w: 480, h: 270 },
    6:  { w: 320, h: 180 },
    8:  { w: 240, h: 135 },
    10: { w: 192, h: 108 }
};

// 发送请求裁剪时：
ws.send(JSON.stringify({
    type: 'setViewport',
    deviceIds: Array.from(VISIBLE_DEVICE_IDS),
    cropCols: currentCols, // 4|6|8|10
    cropSize: LAYOUT_CROP_SIZES[currentCols]
}));
```

### 3.3 服务端修改（server.js）

每 ws 维护视口订阅：
```js
const viewportSubscriptions = new Map(); // ws → { deviceIds: Set, cropCols, cropSize }

// 接受 setViewport 消息
case 'setViewport':
    viewportSubscriptions.set(ws, {
        deviceIds: new Set(data.deviceIds),
        cropCols: data.cropCols || 4,
        cropSize: data.cropSize || { w: 480, h: 270 }
    });
    break;

// 在 ws.on('close') 清理
viewportSubscriptions.delete(ws);
```

帧转发时过滤（在现有的帧广播循环中）：
```js
// 发送帧给主页面时
for (const [ws, vpData] of viewportSubscriptions) {
    if (!vpData.deviceIds.has(deviceId)) continue; // 不可见，不发送

    // 按布局裁剪
    const frame = device.frames[0]; // 最新帧
    if (vpData.cropSize) {
        // Level 0=480×270 是原始小图，按需裁剪
        // Level 1/2=压缩帧需用压缩坐标裁剪
        const cropped = await cropToLayout(
            frame, device.maxLevel || 0,
            vpData.cropSize.w, vpData.cropSize.h
        );
        ws.send(cropped);
    } else {
        ws.send(frame); // 不裁剪直接发
    }
}
```

裁剪辅助函数（基于 Level 和列数）：
```js
async function cropToLayout(frameBuffer, level, targetW, targetH) {
    const sharp = require('sharp');
    if (level === 0) {
        // 480×270 小图，直接 resize
        return sharp(frameBuffer).resize(targetW, targetH).toBuffer();
    }
    // Level 1/2 压缩帧按比例裁剪
    const compressedW = level === 1 ? 853 : 1280;
    const compressedH = level === 1 ? 720 : 1080;
    const origW = level === 1 ? 1280 : 1920;
    const ratio = compressedW / origW;
    const cropW = Math.floor(targetW * ratio);
    const cropH = Math.floor(targetH * ratio);

    return sharp(frameBuffer)
        .extract({ left: 0, top: 0, width: cropW, height: cropH })
        .resize(targetW, targetH)
        .webp({ quality: 30 })
        .toBuffer();
}
```

### 3.4 预览打开/关闭时的处理
```js
// preview.html 打开时
ws.send({ type: 'setLevel', deviceId, level: 2 });
// main.html: 停止该设备的 rAF loop（节省CPU）

// preview.html 关闭时
ws.send({ type: 'setLevel', deviceId, level: 1 }); // 回退
// main.html: 先恢复缓存截图显示 → 重新订阅 → 启动 rAF loop
```

---

## 4. 带宽计算汇总

### 场景：60设备 + 15台监控墙(4×4) + 1设备预览
| 阶段 | 主页 | 监控墙 | 预览 | 总计 | 节省 |
|------|------|--------|------|------|------|
| 当前 | 45.0M | 15.0M | 2.0M | 62.0M | 0% |
| +Step2 | 45.0M | 3.6M | 2.0M | 50.6M | 18% |
| +Step1+2 | 45.0M | 3.6M | 0.5M | 49.1M | 21% |
| +全三步(4列) | 7.2M | 3.6M | 0.5M | **13.3M** | **79%** |
| +全三步(10列) | 12.0M | 3.6M | 0.5M | **17.1M** | **72%** |

---

## 5. 实施优先级与预估

| 步骤 | 难度 | 时间估 | 收益 | 顺序 |
|------|------|--------|------|------|
| Step 2 监控墙裁剪 | 低 | 1-2h | 18% | 先做 |
| Step 1 流级别系统 | 中 | 2-3h | 消除bug+3% | 第二 |
| Step 3 视口懒加载 | 高 | 4-6h | 58% | 最后 |

---

## 6. 关键文件清单

| 文件 | 路径 | 改动内容 |
|------|------|---------|
| server.js | `D:\ScreenWall2\server\server.js` | Step1: deviceMaxLevel/updateLevel/subscribeLevel；Step2: cropFrame；Step3: viewportSubscriptions/cropToLayout |
| client.py | `D:\ScreenWall2\client\client.py` | Step1: setLevel消息处理，target_width/height变量替换硬编码分辨率 |
| main.html | `D:\ScreenWall2\server\public\main.html` | Step1: setLevel替代startHQ；Step3: IntersectionObserver/+setViewport |
| monitor-wall.html | `D:\ScreenWall2\server\public\monitor-wall.html` | Step1: setLevel替代startHQ；Step2: 报告布局给服务端 |
| preview.html | `D:\ScreenWall2\server\public\preview.html` | Step1: 打开/关闭时发setLevel |

---

## 7. 执行 Checklist（给其他助理）

### Step 2 执行清单（先做）
- [ ] 在 server.js 添加 cropFrame 函数
- [ ] 在 server.js 添加 wallLayouts Map
- [ ] 修改 subscribeWall 处理：保存布局信息
- [ ] 修改帧转发逻辑：对监控墙订阅者进行裁剪后再发送
- [ ] 前端 monitor-wall.html 上报 cols/rows 布局参数
- [ ] 测试：打开监控墙4×4布局，确认每格只收到裁剪后的小帧
- [ ] Git commit

### Step 1 执行清单（第二）
- [ ] 在 server.js 添加 deviceMaxLevel/deviceSubscribers Maps
- [ ] 在 server.js 添加 updateLevel/subscribeLevel/unsubscribeLevel 函数
- [ ] 在 server.js 的消息处理中添加 setLevel case
- [ ] 在 ws.on('close') 添加 unsubscribeAllLevel 清理
- [ ] 在 client.py 添加 setLevel 消息处理
- [ ] 修改 client.py 截图逻辑使用动态 target_width/height
- [ ] 修改 main.html 发送 setLevel 替代 startHQ
- [ ] 修改 monitor-wall.html 发送 setLevel
- [ ] 修改 preview.html 打开/关闭时发送 setLevel
- [ ] 测试：打开预览→确认客户端切换到1080p→关闭→回退
- [ ] Git commit

### Step 3 执行清单（最后）
- [ ] 在 main.html 添加 IntersectionObserver + VISIBLE_DEVICE_IDS
- [ ] 在 main.html 添加 reportViewport 函数 + debounce
- [ ] 在 main.html 添加 observeCell 绑定
- [ ] 在 main.html 添加 beforeunload 清理
- [ ] 在 server.js 添加 viewportSubscriptions Map
- [ ] 在 server.js 添加 setViewport 消息处理
- [ ] 在 server.js 添加 cropToLayout 函数
- [ ] 修改 server.js 帧转发逻辑：按视口+裁剪过滤
- [ ] 在 ws.on('close') 清理 viewportSubscriptions
- [ ] 测试：200设备主页，滚动确认不可见格子无帧更新
- [ ] Git commit

---

## 附录：裁剪坐标速查

### 主页按列数裁剪
| 列数 | orig W×H | 压缩坐标(Level1) | 压缩坐标(Level2) |
|------|----------|-----------------|-----------------|
| 4 | 480×270 | 320×270 | 320×270 |
| 6 | 320×180 | 213×180 | 213×180 |
| 8 | 240×135 | 160×135 | 160×135 |
| 10 | 192×108 | 128×108 | 128×108 |

### 监控墙格子裁剪（Level1=853×720）
| 布局 | 每格orig | 每格compressed |
|------|----------|----------------|
| 2×2 | 640×360 | 427×360 |
| 3×3 | 427×240 | 284×240 |
| 4×4 | 320×180 | 213×180 |

> 注：压缩坐标 = orig × (853/1280) = orig × 0.6664 ≈ floor